@model Model.EF.ProcessItemCustomer

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Theme3.cshtml";
    var ListCustomer = (List<Model.View.Dropdownlist2>)ViewBag.Customer;
    var Customer = new List<SelectListItem>();
    Customer.Add(new SelectListItem
    {
        Value = "0",
        Text = "Chọn Khách hàng",
    });
    foreach (var item in ListCustomer)
    {
        Customer.Add(new SelectListItem
        {
            Text = item.ID + ": " + item.Name,
            Value = item.ID,
        });
    }
    var ListItem = (List<Model.View.Dropdownlist2>)ViewBag.Item;
    var Iteml = new List<SelectListItem>();
    Iteml.Add(new SelectListItem
    {
        Value = "0",
        Text = "Chọn Item",
    });
    foreach (var item in ListItem)
    {
        Iteml.Add(new SelectListItem
        {
            Text = item.ID + ": " + item.Name,
            Value = item.ID,
        });
    }

}
@section jsFooter
{
    <script src="/Assets/plugin/ckfinder/ckfinder.js"></script>
    <script>
        $(function () {
            $('.select2').select2()
        })
    </script>
}
<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<link href="~/Assets/js/jquery-ui.css" rel="stylesheet" />
<div class="card card-primary">
    <div class="card-header">
        <h3 class="card-title">Sửa Item Customer</h3>
    </div>
    @using (Html.BeginForm("Edit", "ProcessItemCustomer", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="card-body">
            <div class="row">
                <div class="col-md-12">
                    <h3>Thông tin</h3>
                    <div class="row" style="background: #f6fcfd; padding-bottom: 15px;">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label class="control-label col-md-12">Item ID</label>
                                @Html.TextBoxFor(model => model.Item, new { @ID = "ItemName", @class = "form-control", @readonly = true })
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label class="control-label col-md-12">Item Name</label>
                                @Html.TextBoxFor(model => model.ItemName, new { @ID = "ItemName", @class = "form-control", @readonly = true })
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label class="control-label col-md-12">Customer ID</label>
                                @Html.TextBoxFor(model => model.Customer, new { @ID = "ItemName", @class = "form-control", @readonly = true })
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label class="control-label col-md-12">Customer Name</label>
                                @Html.TextBoxFor(model => model.CustomerName, new { @ID = "CustomerName", @class = "form-control", @readonly = true })
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="card-footer">
            <div class="col-md-4">
                <input type="submit" value="Xoá" class="btn btn-danger" />
            </div>
        </div>
    }
</div>

