@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Theme3.cshtml";
    var info3 = (List<Model.View.Report5>)ViewBag.info3;

}

<div class="card card-primary">
    <div class="card-header">
        <h3 class="card-title"> Thông Tin Chi Tiết Mode Lỗi Kế Hoạch SX - Số Phiếu: <strong> </strong></h3>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-12">

                <div class="row" style="padding-top:5px;margin:20px;">
                    <table class="table table-striped table-bordered table-hover" id="gvCustomers">
                        <thead>
                            <tr>
                                <th>Công đoạn</th>
                                <th>Tổng số lượng OK</th>
                                <th>Tổng số lượng NG</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in info3)
                            {
                                <tr>
                                    <td>@item.Name</td>
                                    <td>@item.Value1</td>
                                    <td>@item.Value1</td>
                                </tr>
                            }

                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>
</div>

