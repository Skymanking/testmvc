@{
    ViewBag.Title = "RequestForm";
    Layout = "~/Views/Shared/_Theme3.cshtml";
    List<SelectListItem> listVT = new List<SelectListItem>();
    var Type = new List<SelectListItem>();
    Type.Add(new SelectListItem
    {
        Value = "0",
        Text = "Sản phẩm mới",
    });
    Type.Add(new SelectListItem
    {
        Value = "1",
        Text = "Cải tiến",
    });
}
<div class="row">
    <div class="col-md-6">
        <label class="control-label">Lý do</label>
        <input type="text" class="form-control" id="" placeholder="Lý do" value="Gia công hàng mới">
    </div>
    <div class="col-md-2">
        <label class="control-label">Loại</label>
        <select class="form-control" disabled>
            @foreach (var item in Type)
            {
                <option value="@item.Value">@item.Text</option>
            }

        </select>
    </div>
    <div class=" col-md-4">
        <label class="control-label">Chọn vật tư muốn đề nghị</label>
        <div class='input-group'>
            <select class="form-control">
                <option>A000000001</option>
            </select>
        </div>
    </div>
    <div class="col-md-12">
        <label class="control-label">Ghi chú</label>
        <textarea class="form-control" rows="3"></textarea>
    </div>
    <br />

</div>
<div class="row">
    <div class="col-md-12">
        <label class="control-label col-md-12">Ý kiến của Giám Đốc</label>
        <input class="form-control" placeholder="" />
    </div>
    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th class="col-md-3" style="width:20%;">QA/QC</th>
                    <th class="col-md-3" style="width:20%;">R&D</th>
                </tr>
            </thead>
            <tbody>
                <tr id="row_0">
                    <td style="background: #f2fbe9;"><input type="checkbox" checked style="height: 60px; width: 60px;" /></td>
                    <td style="background: #f2fbe9;"><input type="checkbox" checked style="height: 60px; width: 60px;" /></td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
<button type="submit" class="btn btn-primary" onclick="">Tạo phiếu</button>
