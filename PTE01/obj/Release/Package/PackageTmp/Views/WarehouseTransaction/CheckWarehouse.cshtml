
@{
    ViewBag.Title = "CheckWarehouse";
    Layout = "~/Views/Shared/_Theme3.cshtml";
    DateTime homnay = DateTime.Now;
    List<Model.EF.WHLot2> full = ViewBag.full;
    string ngaythangnam = "ReportXN" + DateTime.Now.ToUniversalTime();
    List<Model.EF.Warehouse> cWare = ViewBag.cWarehouse;

    List<SelectListItem> listWH = new List<SelectListItem>();
    listWH.Add(new SelectListItem
    {
        Text = "Tất cả",
        Value = "",
    });
    foreach (var item in cWare)
    {
        listWH.Add(new SelectListItem
        {
            Text = item.Name,
            Value = item.ID,
        });
    }

    var ListLo = new List<SelectListItem>();
    ListLo.Add(new SelectListItem
    {
        Text = "Tất cả",
        Value = "",
    });
    var ListLocation = new List<SelectListItem>();
    ListLocation.Add(new SelectListItem
    {
        Text = "Tất cả",
        Value = "",
    });
}
@section jsFooter
{
    <script src="/Scripts/FindLocation.js"></script>
    <script src="/Scripts/FindLot.js"></script>

}
<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<link href="~/Assets/js/jquery-ui.css" rel="stylesheet" />
<div class="card card-primary">
    <div class="card-header">
        <h3 class="card-title">Check Tồn Kho Nâng cao</h3>
        <div class="card-tools">
            <div class="btn-group" style="height:37px">
                <button type="button" class="btn btn-success btn-sm dropdown-toggle" data-toggle="dropdown">
                    More
                </button>
                <div class="dropdown-menu float-right" role="menu">
                    <a class="dropdown-item" href="/WarehouseTransaction/ViewListTrans" style="color:#000">Danh sách phiếu nhập xuất</a>
                    <a class="dropdown-item" href="/WarehouseTransaction/ViewDetailTrans" style="color:#000">Bảng kê nhập xuất kho</a>
                    <a class="dropdown-item" href="/WarehouseTransaction/ViewAmountTrans" style="color:#000">Check tồn kho hiện tại</a>
                    <a class="dropdown-item" href="/WarehouseTransaction/ViewAmountTrans2" style="color:#000">View tồn kho hiện tại</a>
                </div>

            </div>

        </div>

    </div>

    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <div class="callout callout-info">
                    <label class="control-label" style="color: #FF5722;">Vật tư</label><br>
                    <input type="text" class="form-control" id="vattu" name="vattu" placeholder="Mã, tên vật tư" />
                </div>
            </div>
            <div class="col-md-2">
                <div class="callout callout-info">
                    <label class="control-label" style="color: #FF5722;">Kho</label><br>
                    @Html.DropDownList("kho1", listWH, new { @class = "form-control select2 kho" })
                </div>
            </div>
            <div class="col-md-2">
                <div class="callout callout-info">
                    <label class="control-label" style="color: #FF5722;">Vị trí</label><br>
                    @Html.DropDownList("location1", ListLocation, new { @class = "form-control select2 location" })
                </div>
            </div>
            <div class="col-md-2">
                <div class="callout callout-info">
                    <label class="control-label" style="color: #FF5722;">Lô</label><br>
                    @Html.DropDownList("lot1", ListLo, new { @class = "form-control select2 lot" })
                </div>
            </div>
            <div class="col-md-2 text-center">
                <div class="form-group">
                    <a class="btn btn-primary" id="inbaocao">Tìm kiếm</a>
                </div>
            </div>
        </div>
        <script src="~/Scripts/jquery-1.9.1.min.js"></script>
        <script>
            $(document).ready(function () {
                var vattu = $("#vattu").val();
                var wh = $("#kho1").val();
                var location = $("#location1").val();
                var lot = $("#lot1").val();
                $('#inbaocao').attr("href", '/WarehouseTransaction/CheckWarehouse?wh=' + wh + '&vattu=' + vattu + '&lot=' + lot + '&location=' + location);
                $("#inbaocao").focus(function () {
                    var vattu = $("#vattu").val();
                    var wh = $("#kho1").val();
                    var location = $("#location1").val();
                    var lot = $("#lot1").val();
                    $('#inbaocao').attr("href", '/WarehouseTransaction/CheckWarehouse?wh=' + wh + '&vattu=' + vattu + '&lot=' + lot + '&location=' + location);
                });
            });

        </script>
    </div>
</div>


<div class="row">
    <div class="col-md-12">
        <div class="card card-success card-outline">
            <div class="card-header">
                <h3 class="card-title">Kết quả</h3>
                <div class="card-tools">
                    <div class="btn-group" style="height:40px">
                        <button type="button" class="btn btn-success btn-sm dropdown-toggle" data-toggle="dropdown">
                            Tool
                        </button>
                        <div class="dropdown-menu float-right" role="menu">
                            <span class="dropdown-item" onclick="exportTableToExcel('inputdata', '@ngaythangnam')">Export to Excel</span>
                        </div>
                    </div>
                </div>

            </div>
            <div class="card-body">
                @if (full != null)
                {
                    <script>
                        function exportTableToExcel(tableID, filename) {
                            var downloadLink;
                            var dataType = 'application/vnd.ms-excel';
                            var tableSelect = document.getElementById(tableID);
                            var tableHTML = tableSelect.outerHTML.replace(/ /g, '%20');
                            filename = filename ? filename + '.xls' : 'excel_data.xls';
                            downloadLink = document.createElement("a");
                            document.body.appendChild(downloadLink);
                            if (navigator.msSaveOrOpenBlob) {
                                var blob = new Blob(['\ufeff', tableHTML], {
                                    type: dataType
                                });
                                navigator.msSaveOrOpenBlob(blob, filename);
                            } else {
                                downloadLink.href = 'data:' + dataType + ', ' + tableHTML;
                                downloadLink.download = filename;
                                downloadLink.click();
                            }
                        }
                    </script>

                    <div class="table-responsive">
                        <table class="table table-bordered" id="inputdata">
                            <thead>
                                <tr>
                                    <th>Vị trí</th>
                                    <th>Mã Item</th>
                                    <th>Tên Item</th>
                                    <th>Tồn Kho</th>
                                    <th>ĐVT</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in full)
                                {

                                    <tr>
                                        <td>@item.ID</td>
                                        <td>@item.MaterialCode</td>
                                        <td>@item.MaterialName</td>
                                        <td>@item.Amount</td>
                                        <td>@item.MaterialUnit</td>
                                    </tr>
                                }
                            </tbody>
                        </table>


                    </div>
                }
                else
                {
                    <span>chưa có kết quả nào</span>
                }
            </div>
        </div>
    </div>
</div>

